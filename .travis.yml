dist: trusty
sudo: false
mono: none

language: node_js

node_js:
  - stable

branches:
  only:
    - Internationalization

deploy:
  provider: firebase

cache:
  directories:
    - ./node_modules
    
script: 
  - ng build --configuration=production

after_success:
  - firebase deploy