dist: trusty
sudo: false
mono: none

language: node_js

branches:
  only:
    - Internationalization

deploy:
  provider: firebase
   
script: 
  - ng build --configuration=production

after_success:
  - firebase deploy