dist: trusty
sudo: false

language: node_js
node_js:
  - 9

branches:
  only:
    - Internationalization

deploy:
  provider: firebase

script: 
  - ng build --prod

after_success:
  - firebase deploy