#dist: trusty
# sudo: true

language: node_js
node_js:
  14

branches:
  only:
    - Internationalization

deploy:
  provider: firebase
  on:
    branch: Internationalization

script: 
  - ng build --configuration=production
