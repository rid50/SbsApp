dist: trusty
sudo: true

language: node_js
node_js:
  - 14

branches:
  only:
    - Internationalization

deploy:
  provider: firebase

before_script:
  - npm install -g @angular/cli
    
script: 
  - ng build --prod

after_success:
  - firebase deploy