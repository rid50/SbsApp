dist: trusty
sudo: false
mono: none

language: node_js
node_js:
  - node

branches:
  only:
    - Internationalization

deploy:
  provider: firebase
   
script: 
  - ng build --configuration=production

after_success:
  - firebase deploy