language: node_js
node_js:
  14

branches:
  only:
    Internationalization

cache:
  directories:
    ./node_modules

deploy:
  provider: firebase
  on:
    branch: Internationalization

script: 
  ng build --configuration=production
